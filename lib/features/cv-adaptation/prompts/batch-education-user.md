# Formations à Adapter

```json
{educationJson}
```

# Langue de sortie: {targetLanguage}

Traduis les champs `degree` et `field_of_study` de chaque formation dans la langue cible.

**IMPORTANT**:
- Conserve `institution`, `start_date`, `end_date` et `location` tels quels
- Documente TOUTES tes modifications dans `education_modifications[]`
- Si le contenu est déjà dans la langue cible, utilise `action: "kept"`

Réponds en JSON valide.
